# SauceDemo credentials. These values are public (listed on the SauceDemo login page),
# so they work as-is. Copy this file to `.env` before running the tests.
STANDARD_USER=standard_user
LOCKED_USER=locked_out_user
TIMEOUT_TEST_USER=performance_glitch_user
PASSWORD_FOR_ALL=secret_sauce
